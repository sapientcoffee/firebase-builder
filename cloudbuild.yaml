steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'europe-docker.pkg.dev/$PROJECT_ID/firebase', '.']
images:
- 'europe-docker.pkg.dev/$PROJECT_ID/firebase/builder'
tags: ['firebase']